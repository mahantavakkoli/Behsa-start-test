import { CheckListDirectiveDirective } from './check-list-directive.directive';

describe('CheckListDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckListDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
