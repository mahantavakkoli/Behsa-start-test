import { PersianDirectiveDirective } from './persian-directive.directive';

describe('PersianDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PersianDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
