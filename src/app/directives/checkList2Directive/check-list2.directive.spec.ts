import { CheckList2Directive } from './check-list2.directive';

describe('CheckList2Directive', () => {
  it('should create an instance', () => {
    const directive = new CheckList2Directive();
    expect(directive).toBeTruthy();
  });
});
